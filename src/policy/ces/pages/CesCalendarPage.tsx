import { MasterCalendarPage } from '@/policy/pages/MasterCalendarPage';

// Phase 2 ONLY: primitives (SpotlightCard, SurfaceCard, BorderGlow, ToneBadge) applied to event cards + lists
// via MasterCalendar integration for CES views (CesCalendarPage entry). No data/sprint/swimlane changes.
export function CesCalendarPage() {
  return <MasterCalendarPage />;
}
