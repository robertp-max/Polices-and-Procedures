V3 Veil Glass Theme Application — 4 Pages
I'll write these as copy-paste-ready instructions. Each one is just token swaps + CSS overrides, no component rebuilds.

📄 1. Policy Library (/library → LibraryPage.tsx)
TARGET: Apply V3 Veil Glass theme only. Do NOT restructure components or change logic.

BACKGROUND / SURFACES:
- Page container bg → --v3-base-bg: #0F1116
- Main card wrapper → --v3-main-card-bg: #161A22 + border: 1.5px solid #2A2F3A
- Policy cards (grid items) → --v3-surface-1: #1C2029 + border: 1px solid #2A2F3A
- Card hover → --v3-surface-2: #222831 + translateY(-1px) + border-color shift to rgba(255,255,255,0.11)

REMOVE:
- All .glass-panel-lib / .glass-interactive-lib custom classes
- Any gold/amber accent colors → replace with --v3-accent-orange: #E07B2C if warning, otherwise remove
- Any glassmorphism / backdrop-filter on the default card grid (FORBIDDEN per V3 spec)

TEXT:
- Page title → --v3-text-primary: #F1F3F7, 28px, weight 600
- Card titles → --v3-text-primary: #F1F3F7, 15px, weight 500
- Card metadata/descriptions → --v3-text-secondary: #A8B0C0, 13px, weight 400
- Badge/pill text → 11px, weight 500, --v3-text-primary

ACCENTS:
- Active/compliance status badges → --v3-accent-teal: #007970 bg with white text
- Warning/pending badges → --v3-accent-orange: #E07B2C bg with white text
- Search input focus ring → 2px solid #007970
- Links/interactive text → --v3-accent-teal: #007970

BORDERS:
- Between nav and content → broken/interrupted vertical line (dashed, 60-70% opacity)
- Card borders → 1px solid #2A2F3A
- NO continuous solid sidebar borders

SPACING:
- Card grid gap → 16px or 24px
- Card padding → 20px–24px
- Page-level padding inside main card → minimum 32px breathing room

FILTER/SEARCH BAR:
- Background → --v3-surface-1: #1C2029
- Border → 1px solid #2A2F3A
- Input text → --v3-text-primary
- Placeholder → --v3-text-tertiary: #6C7588
📄 2. Forms Library (/forms → FormsPage.tsx)
TARGET: Apply V3 Veil Glass theme only. Do NOT restructure eCIgn entry points or form logic.

BACKGROUND / SURFACES:
- Page bg → --v3-base-bg: #0F1116
- Main container → --v3-main-card-bg: #161A22 + border: 1.5px solid #2A2F3A
- Form cards → --v3-surface-1: #1C2029 + border: 1px solid #2A2F3A
- Card hover → --v3-surface-2: #222831 + translateY(-1px)

REMOVE:
- Any Command Center era styling / gradients
- Any glassmorphism on the default form card grid
- Legacy color variables not in V3 token set

TEXT:
- Page heading "Forms" → --v3-text-primary: #F1F3F7, 28px, weight 600
- Form name on cards → --v3-text-primary: #F1F3F7, 15px, weight 500
- Form description/meta → --v3-text-secondary: #A8B0C0, 13px
- Form type labels → --v3-text-tertiary: #6C7588, 12px

ACCENTS:
- "Create Form" / primary CTA button → --v3-accent-teal: #007970 bg, white text
- "Sign" / action CTA → --v3-accent-orange: #E07B2C bg, white text
- Status indicators (draft/active/signed) → teal for active/signed, orange for pending
- Focus rings → 2px solid #007970

BORDERS:
- Cards → 1px solid #2A2F3A
- Interactive hover → border shifts to rgba(255,255,255,0.11)

SPACING:
- Card grid gap → 16px–24px
- Internal card padding → 20px
- Page breathing room → 32px min from main card edges

SEARCH/FILTER:
- Same treatment as Policy Library (surface-1 bg, #2A2F3A border, tertiary placeholder)
📄 3. Policy Viewer (/library/:policyId → PolicyDetailPage)
TARGET: Apply V3 Veil Glass theme only. Preserve SurfaceCard layout, tabs, metadata grid.

BACKGROUND / SURFACES:
- Page bg → --v3-base-bg: #0F1116
- Main card → --v3-main-card-bg: #161A22 + border: 1.5px solid #2A2F3A
- Content sections (SurfaceCard) → --v3-surface-1: #1C2029 + border: 1px solid #2A2F3A
- Metadata grid cells → --v3-surface-2: #222831 (if elevated) or transparent on surface-1

REMOVE:
- Any residual SCard vs SurfaceCard inconsistencies (use SurfaceCard everywhere, just restyle)
- Teal horizontal rules → replace with broken/dashed lines at 60-70% opacity
- Any glass/blur on the detail view (it's not a Veil)

TEXT:
- Policy title → --v3-text-primary: #F1F3F7, 22px, weight 600
- Section headers → --v3-text-primary: #F1F3F7, 18px, weight 600
- Body content → --v3-text-primary: #F1F3F7, 14-15px, weight 400, line-height 1.45
- Metadata labels → --v3-text-secondary: #A8B0C0, 13px
- Metadata values → --v3-text-primary: #F1F3F7, 14px

TABS:
- Tab container bg → transparent or --v3-surface-1
- Inactive tab text → --v3-text-secondary: #A8B0C0
- Active tab text → --v3-text-primary: #F1F3F7
- Active tab indicator → 2px bottom border --v3-accent-teal: #007970
- Tab hover → --v3-text-primary with subtle bg shift

ACCENTS:
- Status badge (Active/Archived) → teal or orange per status
- "Edit" / "Download" buttons → teal outline or filled
- Compliance tags → small pills, --v3-surface-2 bg, teal text

BORDERS:
- Section separators → broken/dashed horizontal lines, rgba(255,255,255,0.08)
- Card borders → 1px solid #2A2F3A

SPACING:
- Generous: 24px between sections, 32px page padding, 16px metadata grid gap
📄 4. Onboarding (/onboarding-v2/* + /journey/*)
TARGET: Apply V3 Veil Glass theme to unify both V2 and Journey variants.
Do NOT change step logic, form validation, or workflow progression.

BACKGROUND / SURFACES:
- Page bg → --v3-base-bg: #0F1116
- Main onboarding card → --v3-main-card-bg: #161A22 + border: 1.5px solid #2A2F3A
- Step cards / form sections → --v3-surface-1: #1C2029 + border: 1px solid #2A2F3A
- Progress area → --v3-surface-2: #222831

REMOVE:
- Journey V1 "cinematic dark glass" treatment (heavy gradients, dramatic glassmorphism)
- V2 "light professional audit-grade" styling
- Both replaced by V3 flat + border aesthetic
- Any backdrop-filter/blur on default step views

TEXT:
- Step title → --v3-text-primary: #F1F3F7, 22px, weight 600
- Step description → --v3-text-secondary: #A8B0C0, 14px, weight 400
- Form labels → --v3-text-secondary: #A8B0C0, 13px, weight 500
- Form input text → --v3-text-primary: #F1F3F7, 14px
- Placeholder text → --v3-text-tertiary: #6C7588

PROGRESS INDICATOR:
- Progress bar track → --v3-surface-2: #222831
- Progress bar fill → --v3-accent-teal: #007970
- Step numbers (completed) → --v3-accent-teal bg, white text
- Step numbers (current) → --v3-accent-orange: #E07B2C border, orange text
- Step numbers (future) → --v3-surface-2 bg, --v3-text-tertiary text

FORM INPUTS:
- Input bg → --v3-surface-1: #1C2029
- Input border → 1px solid #2A2F3A
- Input focus → border 2px solid #007970
- Input hover → border-color rgba(255,255,255,0.11)

BUTTONS:
- "Next" / primary → --v3-accent-teal: #007970 bg, white text
- "Back" / secondary → transparent, --v3-text-secondary, border: 1px solid #2A2F3A
- "Submit" / final → --v3-accent-orange: #E07B2C bg, white text

SPACING:
- Step sections → 24px gap
- Form field spacing → 16px between fields
- Page padding → 32px breathing room
- Button row → 24px top margin, right-aligned

MOBILE (critical — currently rated 4/10):
- Stack to single column below 768px
- Progress indicator → horizontal compact bar (not vertical stepper)
- Card padding reduces to 16px
- Touch targets minimum 44px height
