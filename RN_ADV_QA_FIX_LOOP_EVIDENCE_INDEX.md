# RN_ADV_QA_FIX_LOOP_EVIDENCE_INDEX.md

## Purpose
Index of all evidence produced during the loop: logs, screenshots (descriptions/paths), git outputs, search results, runtime notes.

Per pass:
- Git outputs
- Build/lint/test logs (paths and summaries)
- Runtime route evidence (titles, console, "screenshot" notes)
- Search outputs (GAO flags, PHI, metadata presence)
- Module specific findings

Initial setup complete. Baseline captured in loop-baseline.txt (branch, HEAD).

Awaiting QA runs to populate evidence per pass.
