node_modules
dist
do-not-merge/
*.mov
*.mp4
*.mp3
*.docx

# Compiled TS emit accidentally written into the source tree.
# This is a TS+Vite project (noEmit:true); any *.js under src/ is stale
# output that SHADOWS the .tsx (Vite resolves .js before .tsx) and hides
# real changes. Never commit these. Type-check with `npm run build`, not
# raw `tsc <file>` (which ignores noEmit and emits these).
src/**/*.js
*.tsbuildinfo

# Agent run litter
build-*.log
agent-tools/

# Reconciliation run output; keep the durable Markdown reports only.
audit/recon-2026-08-03/agents/*.log
audit/recon-2026-08-03/agents/*.exit

# Environment & secrets
.env
.env.*.local
.env.local

# Service-account credentials (NEVER commit)
docs/keys/
*service-account*.json
*credentials*.json
*.private.json
server/credentials/*.json
!server/credentials/.gitkeep
!server/credentials/README.md

# Compliance Intelligence local index (rebuild with `npm run ia:index`)
.cache/

# Temporary scratch files
tmp-*.json
tmp-*.log
.vercel

# PM local persistence (JSON files; regenerated from in-memory store)
data/pm/

# Brad append-only generated-object store (runtime data; not source)
data/brad/


# Trash/archive folder
Bin-(thrash)/
Bin-(thrash)/**

# Local project intelligence / scratch exports
Project_Intelligence/
Project_Intelligence/**

# Local screenshots, UAT outputs, generated reports
**/screenshots/
**/uat-results/
**/uat-html-report/
**/downloads/

# Local current-state exports
current_state_*.md
current_state_*.txt
*_part1.md
*_part2.md
*_part3.md
*_part4.md
*_part5.md

# Python virtualenv — run venv OUTSIDE the repo; never commit it
.venv/

# CDK synth output
infra/**/cdk.out/

# Dev/build scratch litter (keep the tree clean)
npm-run-*
build_*.txt
build*.log
*.pid
temp-*
current-*.png
fresh-*.png

# CI-ION OASIS-E2 SOC simulator narration audio (~950MB of WAVs).
# Built from C:\AI\Git\training\CI-ION\CI-ION_OASIS-E2_SOC (npm run build,
# then copy dist/* into public/advanced-training/oasis-e2-soc/). Re-copy
# the assets when deploying; too large to commit, same as media above.
public/advanced-training/oasis-e2-soc/assets/*.wav

# Real approved-users allowlist (PII) — image build copies it; never commit
config/approved-users.csv
