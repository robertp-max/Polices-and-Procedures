# Detailed Captions for 54 Screenshots

These are detailed descriptions for each of the 54 unique pageview screenshots captured from the prototype.

## 01-achc-crosswalk.png

This screenshot captures the **achc crosswalk** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 02-achc-survey.png

This screenshot captures the **achc survey** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 03-admin-groups.png

This screenshot captures the **admin groups** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 04-admin-permissions.png

This screenshot captures the **admin permissions** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 05-admin-roles.png

This screenshot captures the **admin roles** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 06-admin-users.png

This screenshot captures the **admin users** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 07-appendix-f.png

This screenshot captures the **appendix f** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 08-artifact-viewer.png

This screenshot captures the **artifact viewer** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 09-audit-mode.png

This screenshot captures the **audit mode** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 10-brad.png

This screenshot captures the **brad** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 11-ces-board.png

This screenshot captures the **ces board** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 12-ces-calendar.png

This screenshot captures the **ces calendar** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 13-ces-reports.png

This screenshot captures the **ces reports** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 14-clinician-detail.png

This screenshot captures the **clinician detail** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 15-clinicians.png

This screenshot captures the **clinicians** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 16-dashboard.png

This screenshot captures the **dashboard** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 17-drawer-system.png

This screenshot captures the **drawer system** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 18-ecign-workspace.png

This screenshot captures the **ecign workspace** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 19-evidence-center.png

This screenshot captures the **evidence center** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 20-form-viewer.png

This screenshot captures the **form viewer** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 21-forms-library.png

This screenshot captures the **forms library** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 22-framework.png

This screenshot captures the **framework** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 23-generic-reference.png

This screenshot captures the **generic reference** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 24-governance.png

This screenshot captures the **governance** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 25-help-center.png

This screenshot captures the **help center** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 26-hubstaff.png

This screenshot captures the **hubstaff** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 27-journey-admin.png

This screenshot captures the **journey admin** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 28-journey-overview.png

This screenshot captures the **journey overview** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 29-journey-v1.png

This screenshot captures the **journey v1** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 30-master-calendar.png

This screenshot captures the **master calendar** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 31-master-controls.png

This screenshot captures the **master controls** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 32-mobile-incident.png

This screenshot captures the **mobile incident** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 33-modal-system.png

This screenshot captures the **modal system** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 34-module-player.png

This screenshot captures the **module player** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 35-my-tasks.png

This screenshot captures the **my tasks** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 36-onboarding-v2-activate.png

This screenshot captures the **onboarding v2 activate** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 37-onboarding-v2-audit.png

This screenshot captures the **onboarding v2 audit** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 38-onboarding-v2-batch.png

This screenshot captures the **onboarding v2 batch** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 39-onboarding-v2-batches.png

This screenshot captures the **onboarding v2 batches** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 40-onboarding-v2-dashboard.png

This screenshot captures the **onboarding v2 dashboard** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 41-onboarding-v2-governance.png

This screenshot captures the **onboarding v2 governance** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 42-patient-detail.png

This screenshot captures the **patient detail** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 43-patients.png

This screenshot captures the **patients** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 44-policy-detail.png

This screenshot captures the **policy detail** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 45-policy-library.png

This screenshot captures the **policy library** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 46-policy-lifecycle.png

This screenshot captures the **policy lifecycle** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 47-popover-system.png

This screenshot captures the **popover system** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 48-staffing-calendar.png

This screenshot captures the **staffing calendar** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 49-supervisor.png

This screenshot captures the **supervisor** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 50-surveyor-viewer.png

This screenshot captures the **surveyor viewer** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 51-system-docs.png

This screenshot captures the **system docs** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 52-user-guide.png

This screenshot captures the **user guide** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 53-workflow-swimlane.png

This screenshot captures the **workflow swimlane** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.

## 54-workflows.png

This screenshot captures the **workflows** pageview. It is one of the interactive prototypes in the CareIndeed Home Health redesign, demonstrating the UI components (metrics, tables, cards, progress bars, navigation) for that specific area of the compliance and operations platform. The left sidebar shows the full catalog of 54 views.


