import { chromium } from "playwright";
import fs from "fs";
import path from "path";
const base = "http://localhost:5173";
const outDir = path.join(process.cwd(), "tmp-screenshots");
fs.mkdirSync(outDir, { recursive: true });
const browser = await chromium.launch({ headless: true });
const page = await browser.newPage();
await page.goto(base + "/journey", { waitUntil: "domcontentloaded", timeout: 30000 });
await page.waitForTimeout(3000);
await page.screenshot({ path: path.join(outDir, "journey-overview-achc-split.png"), fullPage: true });
await browser.close();
console.log("Split ACHC screenshot captured.");
